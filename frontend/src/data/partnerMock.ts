/**
 * 列表中 Porten 官方入口（首页"Porten 伙伴"卡片）使用的展示名。
 * 多助手模型中，详情页顶部会取到具体助手自己的 name（从 API 拉取）。
 */
export const partnerName = "Porten 伙伴";

/**
 * 首页入口卡片的占位头像。暂无图片资源时回退到 null，
 * 实际渲染端按文字占位处理。
 */
export const partnerAvatar: string | null = null;
